-- Script to compute the score average of all records in the table "second_table" of the database provided.
-- Querry to compute the score average of all records in the table "second_table" of the database provided.
SELECT AVG(score) AS "avrage" FROM second_table;
